<div class="col-lg-12">
  <div class="row">
    <div class="panel panel-default">
        <div class="panel-heading panel-action-inline">
          Alimentadores registrados ({{model.readyFeeders.length}})
          {{#link-to 'feeder.new' class="btn btn-default btn-primary"}}Criar novo Alimentador{{/link-to}}
        </div>

        <div class="panel-body">
          {{#each model.readyFeeders as |feeder|}}
            {{feeder-item feeder=feeder needRefresh=(route-action 'needRefresh')}}
          {{else}}
            {{#no-data}}
              <p>Ligue um alimentador na rede. Ele irá aparecer na sessão de configuração.Em seguida você deve configurá-lo.</p>
            {{/no-data}}
          {{/each}}
        </div>
    </div>
  </div>
</div>

{{#if model.needSetupFeeders}}
  <div class="col-lg-12">
    <div class="row">
      <div class="panel panel-default">
          <div class="panel-heading panel-action-inline">
            Alimentadores aguardando configuração ({{model.needSetupFeeders.length}})
          </div>

          <div class="panel-body">
            {{#each model.needSetupFeeders as |feeder|}}
              {{feeder-item feeder=feeder needRefresh=(route-action 'needRefresh')}}
            {{/each}}
          </div>
      </div>
    </div>
  </div>
{{/if}}
